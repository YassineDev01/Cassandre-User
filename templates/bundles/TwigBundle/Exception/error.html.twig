


<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ status_code }} - Cassandre</title>
    <!-- Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* General fade-in animation for the main container */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        .animate-fade-in {
            animation: fadeIn 0.8s ease-out forwards;
        }

        /* Animation for the "404" text */
        @keyframes bounceIn {
            0%, 20%, 40%, 60%, 80%, 100% {
                transition-timing-function: cubic-bezier(0.215, 0.61, 0.355, 1);
            }
            0% {
                opacity: 0;
                transform: scale3d(.3, .3, .3);
            }
            20% {
                transform: scale3d(1.1, 1.1, 1.1);
            }
            40% {
                transform: scale3d(.9, .9, .9);
            }
            60% {
                opacity: 1;
                transform: scale3d(1.03, 1.03, 1.03);
            }
            80% {
                transform: scale3d(.97, .97, .97);
            }
            100% {
                opacity: 1;
                transform: scale3d(1, 1, 1);
            }
        }
        .animate-bounce-in {
            animation: bounceIn 1.2s ease-out forwards;
        }

        /* A subtle wiggle for an icon or illustrative element */
        @keyframes wiggle {
            0%, 7% {
                transform: rotateZ(0);
            }
            15% {
                transform: rotateZ(-15deg);
            }
            20% {
                transform: rotateZ(10deg);
            }
            25% {
                transform: rotateZ(-10deg);
            }
            30% {
                transform: rotateZ(6deg);
            }
            35% {
                transform: rotateZ(-4deg);
            }
            40%, 100% {
                transform: rotateZ(0);
            }
        }
        .animate-wiggle {
            animation: wiggle 2s infinite ease-in-out; /* Infinite loop */
        }
    </style>
</head>
<body class="bg-gradient-to-r from-purple-500 to-indigo-600 flex items-center justify-center min-h-screen text-white p-4">

    <div class="text-center animate-fade-in bg-white bg-opacity-10 backdrop-filter backdrop-blur-lg rounded-2xl p-10 max-w-xl mx-auto shadow-2xl border border-white border-opacity-20">
        <!-- Animated 404 Text -->
        <div class="relative mb-6">
            <span class="text-9xl font-extrabold text-blue-300 block animate-bounce-in leading-none">{{ status_code }}</span>
            <!-- A subtle illustrative element -->
            <span class="absolute top-0 right-1/4 transform -translate-x-1/2 -translate-y-1/2 text-5xl animate-wiggle" style="color: #fca5a5;">
                <span role="img" aria-label="confused face">😵</span>
            </span>
        </div>

        <h1 class="text-4xl md:text-5xl font-bold mb-4">Oops! {{ status_text }}</h1>

        {% if status_code == 404 %}
            <p class="text-lg md:text-xl text-white text-opacity-80 mb-8">La page que vous cherchez n’existe pas.</p>
        {% elseif status_code == 403 %}
            <p class="text-lg md:text-xl text-white text-opacity-80 mb-8">Accès refusé. Vous n’avez pas la permission d’accéder à cette page.</p>
        {% else %}
            <p class="text-lg md:text-xl text-white text-opacity-80 mb-8">Une erreur inattendue est survenue. Veuillez réessayer plus tard.</p>
        {% endif %}


        <a href="/" class="inline-block bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-8 rounded-full shadow-lg transition duration-300 ease-in-out transform hover:scale-105">
            Retour à l’accueil
        </a>
        <a href="javascript:history.back()" class="inline-block ml-4 bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 px-8 rounded-full shadow-lg transition duration-300 ease-in-out transform hover:scale-105">
            Retour en arrière
    </div>

</body>
</html>